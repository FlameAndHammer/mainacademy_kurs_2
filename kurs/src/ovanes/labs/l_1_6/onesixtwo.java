package ovanes.labs.l_1_6;
import java.util.Scanner;
/**
 * Created by User on 23.03.2017.
 */
public class onesixtwo {
    public static void main(String[] args) {
    /*    int[] array = new int[]{10, 21, 5, 22, 9, 29, 25, 22, 11, 14, 8, 14};
        int max = array[0]; // начальная точка поиска максимального значения в нулевом элементе массива
        int min = array[0]; // начальная точка описка минимального значения
        for (int i = 0; i < array.length; i++) // int i  переменный номер ячеек массива, i < длины массива, иттерация до конца массива
            for (int j = 0; i < array.length; i++){ // вводится отдельная переменная  j так как i  занят. удивляет, что в остальных еременных  i  устраивает компилятор
                if (max < array[i]) max = array[i]; // если переменная мах меньше текущего значения i, то остается значение i  как большее
                if (min > array[i]) min = array[i];
            }
        System.out.println("Min = " + min);
        System.out.println("Max = " + max);*/
        System.out.println("enter your favorite numbers -");
        Scanner sc = new Scanner(System.in);

    }
}

